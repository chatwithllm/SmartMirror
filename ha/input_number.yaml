# Smart Mirror — input_number helpers
# `input_number: !include ha/input_number.yaml`. See BACKEND_SPEC.md §3.3.

mirror_override_minutes:
  name: Mirror Override Timeout
  min: 5
  max: 240
  step: 5
  initial: 60
  unit_of_measurement: min
